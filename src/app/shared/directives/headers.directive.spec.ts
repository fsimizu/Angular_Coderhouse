import { HeadersDirective } from './headers.directive';

describe('HeadersDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadersDirective();
    expect(directive).toBeTruthy();
  });
});
